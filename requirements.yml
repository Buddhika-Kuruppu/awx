---
collections:
  # Core Ansible collections
  - name: ansible.posix
    version: ">=1.5.0"
  
  - name: community.general
    version: ">=7.0.0"
  
  - name: community.mysql
    version: ">=3.7.0"
  
  - name: community.docker
    version: ">=3.4.0"
  
  - name: community.crypto
    version: ">=2.14.0"

roles:
  # Add any external roles here if needed
  # - name: geerlingguy.docker
  #   version: ">=6.0.0"