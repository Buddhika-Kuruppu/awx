{% for upstream in upstream_servers %}
upstream {{ upstream.name }} {
    least_conn;
    server {{ upstream.server }} weight={{ upstream.weight | default(1) }} max_fails=3 fail_timeout=30s;
    keepalive 32;
}
{% endfor %}